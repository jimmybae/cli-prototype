define(
    function() {
        var GlobalizationView = GMobileJS.View.extend({
            el: '#content',
            template: 'GlobalizationTemplate'
        });
        return GlobalizationView;
    }
);